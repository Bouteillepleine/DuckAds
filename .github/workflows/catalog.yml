name: catalog

on:
  schedule:
    - cron: "17 5 * * 1"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: every catalog source still resolves and parses
        run: bash scripts/check-catalog.sh
